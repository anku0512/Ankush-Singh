import React from "react";
import { Container, Title } from "@mantine/core";

function Contact(props) {
  return (
    <Container size="responsive" style={{ marginTop: "2em" }}>
      <Title className={"mainHeading"}>Contact</Title>
    </Container>
  );
}

export default Contact;
